<html lang="en">
<head>
    <meta http-equiv="content-type" content="text/html; charset=iso-8859-1">
    <title>SUMMARY for GCC RELEASE (ARCH on HOST)</title>
</head>
<body>

<h1>Summary for GCC RELEASE (ARCH on HOST)</h1>

<p>The compiler is now installed under <tt>TARGET_ROOT/COMPILER-HOST</tt>.

<h2>Setting PATH</h2>

<p>To launch it, you must put <tt>TARGET_ROOT/COMPILER-HOST/bin</tt> in front
of your <tt>PATH</tt> environment variable. The following command
enables you to do this in both <tt>bash</tt> and <tt>zsh</tt>:

<pre>
export PATH=TARGET_ROOT/COMPILER-HOST/bin:$PATH</tt>
</pre>

</body>
</html>
