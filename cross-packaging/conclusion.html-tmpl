<html lang="en">
<head>
    <meta http-equiv="content-type" content="text/html; charset=iso-8859-1">
    <title>SUMMARY for GCC RELEASE (ARCH)</title>
</head>
<body>

<h1>Summary for GCC RELEASE (ARCH)</h1>

<p>The compiler is now installed under <tt>/opt/gcc-RELEASE</tt>.

<h2>Setting PATH</h2>

<p>To launch it, you must put <tt>/opt/gcc-RELEASE/bin</tt> in front
of your <tt>PATH</tt> environment variable. The following command
enables you to do this in both <tt>bash</tt> and <tt>zsh</tt>:

<pre>
export PATH=/opt/gcc-RELEASE/bin:$PATH</tt>
</pre>

</body>
</html>
